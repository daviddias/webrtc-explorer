exports = module.exports

exports.connect = (dstId, callback) => {
  // TODO
  // establish a channel with another peer, use intentId
  // pipe the received messages on those sockets to the message router
}
